---
# Ansible koleksiyon gereksinimleri
# Kurulum: ansible-galaxy collection install -r requirements.yml
#
# NOT: vmware.vmware koleksiyonu Python 3.10+ gerektirir.
# Bastion Python 3.6 ise community.vmware yeterlidir.
collections:
  - name: community.vmware
    version: ">=4.0.0"
